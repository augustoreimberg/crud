class Car {
    constructor(id, type, brand, name, engine, fuel) {
        (this.id = id),
            (this.type = type),
            (this.brand = brand),
            (this.name = name),
            (this.engine = engine),
            (this.fuel = fuel)
    }
}

module.exports = Car